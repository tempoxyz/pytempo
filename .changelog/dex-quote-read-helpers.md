---
pytempo: minor
---

Added `StablecoinDEX` read helpers `quote_swap_exact_amount_in`, `quote_swap_exact_amount_out`, `tick_to_price`, and `price_to_tick`, so callers can quote swaps and convert between order ticks and prices without sending a transaction.
